# Copy this file to .env for local development if you do not use DATABASE_URL.
# Docker compose can work without this because app/config.py has defaults.

DB_USER=termini
DB_PASSWORD=termini
DB_HOST=localhost
DB_PORT=5432
DB_NAME=termini_db

# Alternative single variable format:
# DATABASE_URL=postgresql+psycopg://termini:termini@localhost:5432/termini_db

SECRET_KEY=CHANGE_ME_IN_PRODUCTION
ACCESS_TOKEN_EXPIRE_MINUTES=60
